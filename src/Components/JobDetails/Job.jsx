
const Job = ({ jobs }) => {
    console.log(jobs)
    const { logo } = jobs;
    return (
        <div >
            <img src={logo} alt="" srcSet="" />
        
            
            
        </div>
    );
};

export default Job;